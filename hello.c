#include <stdio.h>
int main() {
   // printf() displays the string inside quotation
   printf("Hello, World!");
   printf("hello from <dev_bilel> branch");
   printf("hello from <dev_Alaa> branch :)");
   return 0;
}
